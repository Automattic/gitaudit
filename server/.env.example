# GitHub App credentials (required for authentication)
# Create a GitHub App at: https://github.com/settings/apps
# Required permissions: Contents (Read), Issues (Read), Pull requests (Read), Metadata (Read), Members (Read), Email addresses (Read)
# Important: UNCHECK "Expire user authorization tokens" for non-expiring tokens
# Add callback URLs for each environment (e.g., http://localhost:3001/auth/github/callback)
GITHUB_CLIENT_ID=Iv1.your_github_app_client_id
GITHUB_CLIENT_SECRET=your_github_app_client_secret

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your_random_session_secret_here

# Server config
PORT=3001
CLIENT_URL=http://localhost:3000
# ALLOWED_ORIGINS=https://example.com,https://other.example.com  # Comma-separated list of allowed CORS origins (defaults to CLIENT_URL)

# Database path
# Local development: ./codevitals.db
# Railway deployment: /data/codevitals.db
DATABASE_PATH=./codevitals.db
